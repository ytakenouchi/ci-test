name: conftest

on:
  push:
    branches:
      - develop/conftest

jobs:
  pluto:
    name: conf test-OK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Python3 version
        run: python3 -V

