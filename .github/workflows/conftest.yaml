name: conftest

on:
#  push:
#    branches:
#       - main
##      - develop/conftest
#  pull_request:
#    branches:
#       - main
##      - develop/conftest
  workflow_call:
    inputs:
      repository: 
        type: string
      path:
        type: string
    secrets:
      pat:
        required: true

#条件  
jobs:
  import_policy:
    name: conf test
    runs-on: ubuntu-latest
    steps:
      - name: This repository form ci-test
        uses: actions/checkout@v3
      - name: "ls ci-test after checkout"
        run: ls -la  #./policy エラーになる
      - name: rego repository
        uses: actions/checkout@v3
        with:
          repository: ${{ inputs.repository }}
          path: ${{ inputs.path }}
          token: ${{ secrets.pat}} 
#          token: ${{ secrets.CONFTEST_PAT }} 

      - name: conf install at ci-test
        run: |
          LATEST_VERSION=$(wget -O - "https://api.github.com/repos/open-policy-agent/conftest/releases/latest" | grep '"tag_name":' | sed -E 's/.*"([^"]+)".*/\1/' | cut -c 2-)
          wget "https://github.com/open-policy-agent/conftest/releases/download/v${LATEST_VERSION}/conftest_${LATEST_VERSION}_Linux_x86_64.tar.gz"
          tar xzf conftest_${LATEST_VERSION}_Linux_x86_64.tar.gz
          sudo mv conftest /usr/local/bin/
          conftest --version
          ls -la
      - name: after conf install ls -la
        run: ls -la
      - name: "conf test pod.yaml"
        run: conftest test --policy ./policy ./manifests/Pod.yaml 
##        run: conftest test manifests/Pod.yaml 